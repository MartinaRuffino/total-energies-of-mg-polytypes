# Makefile generated by Maksmakfile for library subs.a
# Machine configuration:  x86_64-unknown-linux-gnu
# Machine mnemonic : INTEL_IFORT
 
# --- compiler switches ---
include Make.inc
 
# --- archive library ---
SUBS = $(TOPDIR)/sx/subs.a
 
ALL:	$(SUBS)
 
init:
	if test -r "$(INAME)"; then \
	env PATH=$(PATH):$(CCOMPDIR) /bin/csh -f $(INAME) ; \
	fi
 
.PRECIOUS:	$(SUBS)
 
subs:	$(SUBS)
 
subs.a:	$(SUBS)
 
# --- Routines requiring special compilation ---
include Make.patch
 
# --- Routines that depend on structures.h ---
# Make with : echo `grep -l "include 'structures.h'" *.f | sed 's/\.f/.o/' `
$(SUBS)(asasx.o lmasa.o):	structures.h
 
$(SUBS):	$(SUBS)(asasx.o) \
	$(SUBS)(asxdev.o) \
	$(SUBS)(asxp0.o) \
	$(SUBS)(asxsgm.o) \
	$(SUBS)(asxsig.o) \
	$(SUBS)(desxdn.o) \
	$(SUBS)(desxdv.o) \
	$(SUBS)(dvsxl.o) \
	$(SUBS)(ibloch.o) \
	$(SUBS)(lmasa.o) \
	$(SUBS)(madmtq.o) \
	$(SUBS)(vbare.o) \
	$(SUBS)(wdiff.o) \
	$(SUBS)(wq0.o) \
	$(SUBS)(wsloc.o) \
	$(SUBS)(wsloca.o) \
	$(SUBS)(wstat.o) \
	$(SUBS)(wtrans.o)
	$(RANLIB) $@
 
clean: 
	-rm -f $(SUBS) Names
