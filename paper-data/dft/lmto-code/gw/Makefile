# Makefile generated by Maksmakfile for library subs.a
# Machine configuration:  x86_64-unknown-linux-gnu
# Machine mnemonic : INTEL_IFORT
 
# --- compiler switches ---
include Make.inc
 
# --- archive library ---
SUBS = $(TOPDIR)/gw/subs.a
 
ALL:	$(SUBS)
 
init:
	if test -r "$(INAME)"; then \
	env PATH=$(PATH):$(CCOMPDIR) /bin/csh -f $(INAME) ; \
	fi
 
.PRECIOUS:	$(SUBS)
 
subs:	$(SUBS)
 
subs.a:	$(SUBS)
 
# --- Routines requiring special compilation ---
include Make.patch
 
$(SUBS):	$(SUBS)(lmfp.o) \
	$(SUBS)(bndfp.o) \
	$(SUBS)(gwinputprodbas.o) \
	$(SUBS)(gworthphi.o) \
	$(SUBS)(iopbme.o) \
	$(SUBS)(makecgr.o) \
	$(SUBS)(makegw.o) \
	$(SUBS)(mtoindex.o) \
	$(SUBS)(prodbas.o) \
	$(SUBS)(prodbaschk.o) \
	$(SUBS)(prodbasme.o) \
	$(SUBS)(prodphi.o) \
	$(SUBS)(rprodbas.o) \
	$(SUBS)(rprodbaschk.o) \
	$(SUBS)(rprodbasi.o) \
	$(SUBS)(vcoulq.o)
	$(RANLIB) $@
 
clean: 
	-rm -f $(SUBS) Names
